{
  "name": "webapp",
  "version": "0.1.0",
  "description": "A minimal Electron application for launch web apps.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --platform=win32 --arch=x64 --overwrite --icon=favicon.ico --out=build WebApp"
  },
  "repository": "https://github.com/tonvieira/webapp",
  "keywords": [
    "WebApp",
    "Electron",
    "quick",
    "launcher"
  ],
  "author": "Everton Murilo Vieira",
  "devDependencies": {
    "electron": "^11.1.1"
  },
  "dependencies": {
    "electron-store": "^6.0.0"
  }
}
